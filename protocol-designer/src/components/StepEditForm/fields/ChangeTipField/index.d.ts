import { DisabledChangeTipArgs } from './getDisabledChangeTipOptions';
import { FieldProps } from '../../types';
type Props = FieldProps & DisabledChangeTipArgs;
export declare const ChangeTipField: (props: Props) => JSX.Element;
export {};
