import * as React from 'react';
export interface FormColumnProps {
    children?: React.ReactNode;
    sectionHeader?: React.ReactNode;
}
export declare const FormColumn: (props: FormColumnProps) => JSX.Element;
