import { LabwareOnDeck } from '../../../step-forms';
interface LabwareHighlightProps {
    labwareOnDeck: LabwareOnDeck;
}
export declare const LabwareHighlight: (props: LabwareHighlightProps) => JSX.Element | null;
export {};
