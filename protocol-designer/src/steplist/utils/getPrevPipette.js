// @flow
import type {
  FormData,
  StepIdType,
} from '../../form-types'

/** returns last used pipette, falls back to left or only pipette
  * when no pipettes were previously used */
export default function getPrevPipette (savedForms: FormData, stepId: StepIdType): string {
  return 'TODO'
}
