*.py[cod]
*.egg
build
htmlcov
*.env
abr_testing/_version.py
slack_app_manifest.yaml

# Secrets / keys — never commit these. Keep templates/*.example.json tracked.
*ssh*
*robot_key*
jiraCredentials.json
