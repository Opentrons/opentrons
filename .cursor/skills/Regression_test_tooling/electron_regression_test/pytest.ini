[pytest]
pythonpath = .
testpaths = tests
addopts =
    -ra
    -s
    --html=test-results/report.html
    --self-contained-html
markers =
    smoke: navigation tests under tests/nav/
    device_cards: hardware card tests under tests/device_cards/
