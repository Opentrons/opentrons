"""Navigation tests — left-panel app areas."""

import pytest

from automation.helpers.test_progress import make_suite_logstart

pytestmark = pytest.mark.smoke

pytest_runtest_logstart = make_suite_logstart("nav")
