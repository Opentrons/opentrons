{
  "$id": "opentronsRobotSchemaV1",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "robotName": {
      "type": "string",
      "enum": ["OT-2 Standard", "OT-3 Standard"]
    },
    "robotId": {
      "type": "string",
      "enum": ["ot-2", "ot-3"]
    }
  },
  "description": "Describes an Opentrons liquid handling robot.",
  "type": "object",
  "required": ["friendlyName", "robotName", "robotId", "otId", "models"],
  "properties": {
    "friendlyName": {
      "description": "A user-facing friendly name for the machine.",
      "type": "string"
    },
    "robotName": {
      "description": "A machine-readable but still printable name for the machine.",
      "type": "#/definitions/robotName"
    },
    "robotId": {
      "description": "A machine-readable-only name for the machine. Intended for use as a reference in other documents.",
      "type": "#/definitions/robotId"
    },
    "otId": {
      "description": "A UUID for the machine type. Intended for use as a reference in other documents.",
      "type": "string"
    },
    "models": {
      "description": "A list of robot models.",
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}
