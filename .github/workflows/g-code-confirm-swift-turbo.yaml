name: "G-Code-Confirm (Swift Turbo)"

on:
  # Run on any change to the api directory
  pull_request:
    paths:
      - 'api/**'
      - 'Makefile'
    branches:
      - '*'
  push:
    paths:
      - 'api/**'
      - 'Makefile'
    branches:
      - '*'

  workflow_dispatch:

jobs:
  confirm-g-code:
    name: 'Confirm G-Code (Swift Turbo)'
    runs-on: 'ubuntu-18.04'
    steps:
      - uses: 'actions/checkout@v2'
      - uses: 'actions/setup-node@v1'
        with:
          node-version: '14'
      - uses: 'actions/setup-python@v2'
        with:
          python-version: '3.7'
      - uses: './.github/actions/python/setup'
        with:
          project: 'api'

      - name: 'Run & Compare to S3'
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.S3_APP_DEPLOY_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.S3_APP_DEPLOY_SECRET }}
          AWS_DEFAULT_REGION: us-east-2
        run: make -C api g-code-swift-turbo-protocol-diff

      - name: "Save Generated Diff"
        if: always()
        uses: actions/upload-artifact@v2
        with:
          name: g-code-swift-turbo-protocol-diff
          path: /tmp/swift-turbo-diff.html
