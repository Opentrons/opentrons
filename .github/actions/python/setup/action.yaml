name: 'Python Setup'
description: 'Set up the environment to handle a monorepo python project'
inputs:
  project:
    description: 'Which project (by subdirectory) to set up'
    required: true
  python-version:
    description: "What Python version to use to create the project's virtual environment"
    required: false
    default: "false"
runs:
  using: 'composite'
  steps:
    - shell: bash
      run: |
        if [[ "${OSTYPE}" =~ "linux" ]]; then
          # WORKAROUND: Remove microsoft debian repo due to https://github.com/microsoft/linux-package-repositories/issues/130. Remove line below after it is resolved
          sudo rm -f /etc/apt/sources.list.d/microsoft-prod.list
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends libsystemd-dev
        fi
    - shell: bash
      run: npm install --global shx@0.3.3
    - shell: bash
      id: check-lockfile
      run: |
        if [ -f "${{ inputs.project }}/uv.lock" ]; then
          echo "lockfile-exists=true" >> $GITHUB_OUTPUT
          echo "cache-glob=${{ inputs.project }}/uv.lock" >> $GITHUB_OUTPUT
        else
          echo "lockfile-exists=false" >> $GITHUB_OUTPUT
          echo "cache-glob=" >> $GITHUB_OUTPUT
        fi
    - uses: astral-sh/setup-uv@v7
      id: setup-uv
      with:
        python-version: ${{ inputs.python-version != 'false' && inputs.python-version || '3.10' }}
        enable-cache: ${{ steps.check-lockfile.outputs.lockfile-exists == 'true' }}
        cache-dependency-glob: ${{ steps.check-lockfile.outputs.cache-glob }}
    - shell: bash
      if: always() && steps.check-lockfile.outputs.lockfile-exists == 'true'
      run: |
        # Ensure cache directory exists to prevent post-job cleanup failures
        # This runs even if previous steps fail (always()) to ensure the directory
        # exists for the setup-uv action's post-job cleanup
        # Use UV_CACHE_DIR which is set by setup-uv, with fallback to default paths
        CACHE_DIR="${UV_CACHE_DIR}"
        if [ -z "$CACHE_DIR" ]; then
          # Fallback to default paths if UV_CACHE_DIR not set
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            CACHE_DIR="D:/a/_temp/setup-uv-cache"
          elif [[ "$RUNNER_OS" == "macOS" ]]; then
            CACHE_DIR="/Users/runner/work/_temp/setup-uv-cache"
          else
            CACHE_DIR="/home/runner/work/_temp/setup-uv-cache"
          fi
        fi
        # Normalize Windows paths (backslashes to forward slashes for bash)
        CACHE_DIR=$(echo "$CACHE_DIR" | tr '\\' '/')
        # Create directory if it doesn't exist (works on both Unix and Windows with bash)
        mkdir -p "$CACHE_DIR" 2>/dev/null || true
        # Create a placeholder file so the directory is not empty
        echo "placeholder" > "$CACHE_DIR/.placeholder" 2>/dev/null || true
    - name: Setup Python Environment
      shell: bash
      run: |
        echo "::notice::Using UV for dependency management"
        START_TIME=$(date +%s)
        # Add linux extra if on Linux and project has linux optional dependency (for systemd-python in performance-metrics)
        LINUX_EXTRA=""
        if [[ "${OSTYPE}" =~ "linux" ]] || [[ "$(uname -s)" == "Linux" ]]; then
          # Check if project has linux optional dependency defined
          if [ -f "${{ inputs.project }}/pyproject.toml" ] && grep -q '\[project.optional-dependencies\]' "${{ inputs.project }}/pyproject.toml" && grep -A 5 '\[project.optional-dependencies\]' "${{ inputs.project }}/pyproject.toml" | grep -q 'linux'; then
            LINUX_EXTRA="--extra linux"
          fi
        fi
        if [ -f "${{ inputs.project }}/uv.lock" ]; then
          cd ${{ inputs.project }} && uv sync --frozen --extra dev $LINUX_EXTRA
        elif [ -f "${{ inputs.project }}/pyproject.toml" ]; then
          cd ${{ inputs.project }} && uv sync --extra dev $LINUX_EXTRA
          # Verify dev dependencies are installed
          uv pip list | grep -E "(mypy|pytest|black)" || echo "Warning: Dev dependencies may not be installed"
        else
          make -C ${{ inputs.project }} setup
        fi
        END_TIME=$(date +%s)
        DURATION=$((END_TIME - START_TIME))
        echo "::notice::UV setup completed in ${DURATION} seconds"
