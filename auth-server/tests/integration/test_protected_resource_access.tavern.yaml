test_name: Accessing protected resources within auth-server

marks:
  - usefixtures:
      - run_server

stages:
  - name: Enable access control enabled so protected resources require a valid token
    request:
      method: PATCH
      url: '{run_server.base_url}/auth/settings/accessControlEnabled'
      json:
        data:
          accessControlEnabled: true
    response:
      status_code: 200

  - name: Accessing the resource without a token should fail
    request:
      method: POST
      url: '{run_server.base_url}/auth/users'
      json:
        data:
          username: testuser
          password: testuserpassword
          fullName: Test User
          accountType: user
    response:
      status_code: 401

  - name: Accessing the resource with a bogus token should fail
    request:
      method: POST
      json:
        data:
          username: testuser
          password: testuserpassword
          fullName: Test User
          accountType: user
      url: '{run_server.base_url}/auth/users'
      headers:
        Authorization: Bearer bogus_token_1234
    response:
      status_code: 401

  - name: Get an access token with insufficient scopes
    request:
      method: POST
      url: '{run_server.base_url}/auth/oauth2/token'
      data:
        client_id: opentrons_app
        grant_type: password
        username: testadmin
        password: testadminpassword
        scope: users.read.others # Omit users.write.
    response:
      status_code: 200
      json:
        access_token: !anystr
        refresh_token: !anystr
        expires_in: !anyint
        token_type: Bearer
        scope: users.read.others
      save:
        json:
          access_token: access_token

  - name: Accessing the resource with an insufficiently-scoped token should fail
    request:
      method: POST
      url: '{run_server.base_url}/auth/users'
      json:
        data:
          username: testuser
          password: testuserpassword
          fullName: Test User
          accountType: user
      headers:
        Authorization: 'Bearer {access_token}'
    response:
      status_code: 403

  - name: Get an access token with sufficient scopes
    request:
      method: POST
      url: '{run_server.base_url}/auth/oauth2/token'
      data:
        client_id: opentrons_app
        grant_type: password
        username: testadmin
        password: testadminpassword
        scope: users.write
    response:
      status_code: 200
      json:
        access_token: !anystr
        refresh_token: !anystr
        expires_in: !anyint
        token_type: Bearer
        scope: users.write
      save:
        json:
          access_token: access_token

  - name: Accessing the resource with a sufficiently-scoped token should succeed
    request:
      method: POST
      url: '{run_server.base_url}/auth/users'
      json:
        data:
          username: testuser_new
          password: testuserpassword
          fullName: Test User
          accountType: user
      headers:
        Authorization: 'Bearer {access_token}'
    response:
      status_code: 201
