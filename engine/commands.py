def parse(string):
	"""
	Takes a command string and parses it based on the rules stated by
	defined commands.

	Returns a dict of named arguments, optimized for being dumped to
	JSON.
	"""
	return None