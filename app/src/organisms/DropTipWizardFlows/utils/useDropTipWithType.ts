// This is the main unifying function for maintenanceRun and fixit type flows.
export function useDTWithType() {
  //const setupCommands = useDropTipSetupCommands(...)
  //const fixitCommands = useDropTipFixitCommands(...)
}
