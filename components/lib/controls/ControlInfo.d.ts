import * as React from 'react';
export interface ControlInfoProps {
    children: React.ReactNode;
    className?: string;
}
export declare function ControlInfo(props: ControlInfoProps): JSX.Element;
