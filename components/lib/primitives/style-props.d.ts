import * as Types from './types';
import type { CSSObject } from 'styled-components';
export declare const styleProps: (props: Types.StyleProps) => CSSObject;
export declare const isntStyleProp: (prop: string | React.ReactText) => boolean;
